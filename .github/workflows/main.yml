name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        uses: actions/checkout@v2
      - run: echo Hello
